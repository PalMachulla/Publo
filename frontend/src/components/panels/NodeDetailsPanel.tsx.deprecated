/**
 * NodeDetailsPanel - DEPRECATED 2024-12-14
 * 
 * This file has been refactored. The original 1486-line component has been:
 * 1. Split into smaller focused components:
 *    - StoryStructureMetadataPanel.tsx (metadata & embeddings)
 *    - AIPromptPanel.tsx (AI prompt configuration)
 *    - GenericNodePanel.tsx (fallback for generic nodes)
 *    - PanelContainer.tsx (reusable sliding panel wrapper)
 * 
 * 2. The new NodeDetailsPanel.tsx is a thin router that:
 *    - Determines node type
 *    - Renders PanelContainer with the appropriate panel inside
 * 
 * Original file preserved here for reference.
 * Delete after verifying new implementation works.
 */

// File content moved to NodeDetailsPanel.tsx.deprecated.bak if you need the full original
